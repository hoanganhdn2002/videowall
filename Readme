wget http://namvision.com/install.sh.zip
unzip -o install.sh.zip 
./install.sh
tmux ls : kiểm tra xem đã tự tạo tmux của videowall chưa, nếu chưa tạo thì gõ thêm lệnh : ./videowall.sh vậy là okay
Sau đấy vào tmux a -t videowall để kiểm tra xem phần mềm đã chạy chưa.
